const internalPackages = [
    'Accessories',
    'Coating package',
    'Coating ala-carte',
    'PPF package',
    'PPF ala-carte',
    'Tint package',
    'Tint ala-carte',
    'Tint + coating package',
].sort();

// export const packages = internalPackages.map((pkg, idx) => ({ name: pkg, code: `pkg-${idx}` }));
export const packages = internalPackages;
